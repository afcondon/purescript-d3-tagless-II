// FFI for WealthHealth component

export function log(message) {
  console.log(message);
}

// TODO: Add D3 visualization rendering functions here
// These will be called from PureScript to update the SVG
