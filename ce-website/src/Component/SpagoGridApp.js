// Show the flare tree container
export function showFlareContainer() {
  const container = document.getElementById('flare-tree-container');
  if (container) {
    container.style.display = 'block';
  }
}
